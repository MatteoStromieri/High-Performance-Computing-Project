#include "gendata.h"

data_t* merge_arrays( data_t*, int, int, int, int, data_t*);

void copy_array(data_t*, data_t*, int, int);

void merge_all(data_t*, int*, int, int);

void merge_gathered_data(data_t*,int,int);
